namespace Project.Infrastructure.Data
{
    /// <summary>
    /// Enum representing game scenes.
    /// The index of each enum value must match the build index in the build settings.
    /// </summary>
    public enum GameScene
    {
        Bootstrap = 0,
        Menu = 1,
        Gameplay = 2
    }
}